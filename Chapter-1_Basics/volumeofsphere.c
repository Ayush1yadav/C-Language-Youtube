#include <stdio.h>
int main(){
    float pi=3.14;
    float radius = 2;
    float volume=(4*pi*radius*radius*radius)/3;
    printf("Volume of sphere is %f when radius is %f and value of pi is %f",volume,radius,pi);
}